import { StyleSheet } from 'react-native'

export const styles = StyleSheet.create({
  container: {
    position: 'absolute',
    marginRight: 145
  },

  logo: {
    width: 100,
    height: 100,
    position: 'absolute',
    justifyContent: 'center',  
    marginRight: -60,
    marginTop: -70
    }
})