import { StyleSheet } from 'react-native'

export const styles = StyleSheet.create({
  container2: {
    position: 'absolute',
  },
  
  logo: {
    width: 130,
    height: 130,
    position: 'absolute',
    justifyContent: 'center',
    alignItems: 'center',
    marginLeft: 50,
    marginTop: -90
  }
})